package com.example.notev.utils

import com.example.notev.ui.AddEditNoteActivity

/**
 * Add or Edit status for [AddEditNoteActivity] to determine is it adding operation or editing.
 */
enum class NoteStatus { ADD, EDIT }